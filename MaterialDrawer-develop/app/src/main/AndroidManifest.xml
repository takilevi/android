<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hu.py82c7.homework.app">

    <uses-permission android:name="android.permission.INTERNET" />

    <application

        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name">
        <!-- android:supportsRtl="true" -->
        <activity
            android:name="hu.py82c7.homework.app.AdvancedActivity"
            android:label="@string/app_name"
            android:theme="@style/MaterialDrawerTheme.TranslucentStatus">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="hu.py82c7.homework.login.LoginActivity" android:theme="@style/AppTheme.Dark" />
        <activity android:name="hu.py82c7.homework.login.SignupActivity" android:theme="@style/AppTheme.Dark" />
    </application>

</manifest>
